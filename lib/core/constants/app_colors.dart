import 'package:flutter/material.dart';

class AppColors {
  // Primary & Accent
  static const Color primary = Color(0xFF4C51BF); // Soft Indigo
  static const Color accent = Color(0xFFFBBF24); // Warm Gold
  static const Color secondary = Color(0xFF818CF8); // Soft Blue-Purple

  // Backgrounds
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Colors.white;
  static const Color scaffoldBackground = Color(0xFFEDEFF3);

  // Text
  static const Color textPrimary = Color(0xFF1A1C30);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFF9CA3AF);

  // Semantic
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Yes/No colors
  static const Color yes = Color(0xFF10B981); // Greenish
  static const Color no = Color(0xFFEF4444); // Reddish
}
